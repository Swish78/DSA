#include<bits/stdc++.h>
using namespace std;

inline void
solve (vector < int >&sol)
{
  stable_sort (arr.begin (), arr.end ());
}

int main (){
  vector <int>sol = { 1, 5, -2, 8, -9, -4, 3, 0, 5, -3 };
  solve (arr);
for (auto e:arr)
    cout << e << "\t";
  return 0;
}
